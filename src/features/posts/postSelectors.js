export const allPosts = (state) => state.posts.posts;
export const loadingPosts = (state) => state.posts.loading;
export const errorPosts = (state) => state.posts.error;
export const filterPosts = (state) => state.posts.filter;
